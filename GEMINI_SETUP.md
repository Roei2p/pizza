# הפעלת יכולות ה-AI (Gemini)

האתר עובד גם בלי זה — שלושת התכונות האלה פשוט לא יופיעו:

1. **צ'אטבוט ללקוחות** (הכפתור השחור העגול עם היהלום, בערימת הכפתורים הצפים)
2. **המלצה אישית** ללקוח חוזר עם עגלה ריקה
3. **עוזר לשרון** בלוח הבקרה שלה (`/#sharon`), שעונה על שאלות לגבי ההזמנות הפעילות

## שלב יחיד: מפתח API חינמי

1. כנסו ל-https://aistudio.google.com/apikey (עם חשבון Google רגיל, בחינם)
2. Create API key → תעתיקו את המפתח שמופיע

## חיבור לאתר

באותו קובץ `.env.gh-pages` שיצרתם עבור Firebase (ראו `FIREBASE_SETUP.md`), תוסיפו שורה:

```
VITE_GEMINI_API_KEY="המפתח שהעתקתם"
```

תשמרו, commit ו-push — זהו.

## ⚠️ חשוב לדעת

המפתח נכנס **ישירות בצד הלקוח** (בקוד ה-JS של האתר) — זו הייתה בחירה מודעת
לפשטות, במקום להקים שרת ביניים שמסתיר אותו. המשמעות: מי שבאמת ירצה, יכול
לחלץ את המפתח מהדפדפן ולנצל את המכסה החינמית שלכם. לפיצרייה קטנה עם תנועה
סבירה זה לא אמור להיות בעיה בפועל — אבל אם בעתיד תבחינו בשימוש חריג
(המפתח "נגמר" מהר מהצפוי), הפתרון הנכון הוא להעביר את הקריאות ל-Gemini
דרך Firebase Cloud Function (כמו שדיברנו לגבי סליקה) כדי שהמפתח לא יהיה
חשוף כלל.

## פיתוח מקומי

`bun run dev` קורא את המפתח מ-`.env.local` (לא נשמר בגיט). בלי מפתח,
התכונות פשוט לא יופיעו — אין צורך במפתח אמיתי כדי שהאתר ירוץ.
