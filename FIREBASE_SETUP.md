# הפעלת ה-backend האמיתי (Firebase)

⚠️ **חשוב:** הזמנות באתר עוברות עכשיו רק דרך Firebase (וואטסאפ הוסר
לגמרי מתהליך ההזמנה). **עד שתשלימו את השלבים כאן, כפתור "שליחת ההזמנה"
באתר החי לא יעבוד** — הלקוחות יראו הודעת שגיאה. חשוב להשלים את זה לפני
או מיד אחרי שהשינוי הזה עולה לאוויר.

אחרי ההגדרה תקבלו: היסטוריית הזמנות אמיתית לפי טלפון (חוצה מכשירים),
ולוח בקרה חי לשרון בכתובת `/#sharon` שבו היא מסמנת התקדמות אמיתית —
וזה מה שהלקוח רואה במעקב ההזמנה שלו.

כל השלבים האלה חד-פעמיים, ורק אתה (או שרון) יכולים לעשות אותם — זה
דורש חשבון Google אמיתי, אני לא יכול ליצור אותו בשבילכם.

## 1. יצירת הפרויקט (חינם)

1. כנסו ל-https://console.firebase.google.com ולחצו "Add project"
2. תנו שם (למשל `sharon-pizza`), אין צורך ב-Google Analytics
3. תישארו על תוכנית ה-Spark החינמית — יותר ממספיק לפיצרייה אחת

## 2. הפעלת Firestore

1. בתפריט הצד: Build → Firestore Database → Create database
2. Production mode
3. מיקום: `europe-west1` (בלגיה, הכי קרוב לישראל)
4. אחרי היצירה: לשונית Rules → תמחקו את מה שיש ותדביקו את כל התוכן של
   הקובץ `firestore.rules` מהריפו הזה → Publish

## 3. הפעלת התחברות לשרון

1. Build → Authentication → Get started
2. בחרו ספק "Email/Password" → הפעילו → Save
3. לשונית Users → Add user → תנו לשרון אימייל וסיסמה אמיתיים שהיא
   תשתמש בהם כדי להיכנס ללוח הבקרה שלה

## 4. חיבור האתר לפרויקט

1. ב-Project settings (גלגל השיניים למעלה) → Your apps → Add app → Web (`</>`)
2. תנו שם, **לא** צריך Firebase Hosting (האתר כבר ב-GitHub Pages)
3. תעתיקו את הערכים מתוך `firebaseConfig` שמופיע שם
4. תיצרו קובץ בשם `.env.gh-pages` בתיקיית הפרויקט (ליד `package.json`)
   עם הערכים האלה (אין בהם שום דבר סודי — זה בטוח לשמור בגיט):

   ```
   VITE_FIREBASE_API_KEY="..."
   VITE_FIREBASE_AUTH_DOMAIN="..."
   VITE_FIREBASE_PROJECT_ID="..."
   VITE_FIREBASE_STORAGE_BUCKET="..."
   VITE_FIREBASE_MESSAGING_SENDER_ID="..."
   VITE_FIREBASE_APP_ID="..."
   ```

5. תשמרו, תעשו commit ו-push לענף — הדיפלוי הבא כבר ידבר עם הפרויקט
   האמיתי שלכם במקום עם הסימולציה

## 5. פעם ראשונה שפותחים את `/#sharon`

Firestore לפעמים מבקש "אינדקס" (index) בפעם הראשונה ששרון פותחת את
הלוח שלה עם פרויקט אמיתי (לא קורה בסביבת הבדיקות המקומית). אם ההזמנות
לא מופיעות, תפתחו את "כלי הפיתוח" בדפדפן (F12) → Console, ותחפשו הודעה
עם קישור שמתחיל ב-`https://console.firebase.google.com/.../indexes?...`
— לחיצה על הקישור ואישור לוקחת כדקה ופותרת את זה לצמיתות.

## פיתוח מקומי (לא חובה לדעת את זה כדי שהאתר יעבוד)

`bun run dev` מתחבר אוטומטית לסימולטור מקומי של Firebase (לא לפרויקט
האמיתי) כשמריצים גם:

```
firebase emulators:start --only firestore,auth
```

זה מאפשר לבדוק שינויים בלי לגעת בנתונים האמיתיים של המסעדה.
